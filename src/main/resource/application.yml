spring:
  application:
    name: danzapp-backend
  datasource:
    url: jdbc:postgresql://localhost:5432/danzapp
    username: danzapp_user
    password: changeme # Cambia esto por tu contraseña real
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update # Crea tablas automáticamente
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

jwt:
  secret: d4nz4pp-s3cr3t-k3y-f0r-jwt-t0k3n-g3n3r4t10n-must-be-32-chars-min
  expiration-ms: 86400000

server:
  port: 8080